Add your launch assets here:

- `hero-background.jpg` for the full-page background image
- `pooly_icon_stacked.png` for the logo inside the top card

The landing page is already wired to those filenames.
