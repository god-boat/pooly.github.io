---
interface Props {
	class?: string;
}

const { class: className } = Astro.props;
---

<span class:list={["hand-note", className]}>
	<slot />
</span>

<style>
	.hand-note {
		font-family: var(--font-hand);
		color: var(--color-note);
		font-size: 0.95rem;
		line-height: 1.2;
	}
</style>
